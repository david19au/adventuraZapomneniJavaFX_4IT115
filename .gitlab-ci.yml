image: maven:3.8

sestaveni:
  script:
    - "mvn javafx:jlink -B"
    - "mvn -Dit.test='\!InventarTest,\!KoupitTest,\!BodnoutTest' install -B"
  artifacts:
    paths:
      - "target/*.zip"
      - "target/*.jar"


